// Create a function that calculates how many minutes have elapsed from midnight until right now.
// The function should still return an accurate answer,
// even if you ran it a few minutes or hours later.


function minSinceMidnight(){
    //create variables for current date and for midnight of the current date
    //make another variable that finds ms in one minute
    //subtract both variables by referencing dates using timeVariable.getTime() for each value
    //return the difference divided by the variable that calculated ms in a minute
}

console.log(minSinceMidnight());
